import React from 'react';

const Input = (title, placeholder) => {
    return (
        <div className="input">
            <input />
            <label>{title}</label>
        </div>
    )
}

export default Input;